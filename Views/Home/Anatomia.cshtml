@{
    ViewData["Title"] = "Anatomia Humana";
}
<h1>@ViewData["Title"]</h1>
<div id="viewer3D" style="width: 100%; height: 600px; background: linear-gradient(to bottom, #e0f7fa, #b2ebf2);"></div>

<p style="text-align: center; margin-top: 20px; color: #0c86c3; font-size: 1.1rem;">Contenido de Anatomia Humana.</p>

@section Scripts {
    <script type="importmap">
      {
        "imports": {
          "three": "https://unpkg.com/three@0.160.0/build/three.module.js",
          "three/addons/": "https://unpkg.com/three@0.160.0/examples/jsm/"
        }
      }
    </script>
    
    <script type="module">
        import * as THREE from 'three';
        import { GLTFLoader } from 'three/addons/loaders/GLTFLoader.js';
        import { OrbitControls } from 'three/addons/controls/OrbitControls.js';

        console.log("Iniciando visualizador 3D...");

        // Esperar a que el DOM esté listo
        document.addEventListener('DOMContentLoaded', function() {
            const container = document.getElementById('viewer3D');
            
            if (!container) {
                console.error("No se encontró el contenedor viewer3D");
                return;
            }

            // Escena
            const scene = new THREE.Scene();
            console.log("Escena creada");

            // Cámara
            const camera = new THREE.PerspectiveCamera(
                45,
                container.clientWidth / container.clientHeight,
                0.1,
                1000
            );
            camera.position.set(0, 2, 5);
            camera.lookAt(0, 0, 0);
            console.log("Cámara configurada");

            // Renderizador
            const renderer = new THREE.WebGLRenderer({ 
                antialias: true,
                alpha: true 
            });
            renderer.setSize(container.clientWidth, container.clientHeight);
            renderer.setPixelRatio(window.devicePixelRatio);
            renderer.setClearColor(0x000000, 0); // Fondo transparente
            container.appendChild(renderer.domElement);
            console.log("Renderer creado");

            // Luces
            const ambientLight = new THREE.AmbientLight(0xffffff, 1.5);
            scene.add(ambientLight);

            const directionalLight1 = new THREE.DirectionalLight(0xffffff, 2);
            directionalLight1.position.set(5, 10, 7);
            scene.add(directionalLight1);

            const directionalLight2 = new THREE.DirectionalLight(0xffffff, 1);
            directionalLight2.position.set(-5, 5, -5);
            scene.add(directionalLight2);
            console.log("Luces añadidas");

            // Controles
            const controls = new OrbitControls(camera, renderer.domElement);
            controls.enableDamping = true;
            controls.dampingFactor = 0.05;
            controls.minDistance = 1;
            controls.maxDistance = 10;
            controls.target.set(0, 0, 0);
            controls.update();
            console.log("Controles configurados");

            // Loader GLB
            const loader = new GLTFLoader();
            console.log("Cargando modelo desde: /dummy3.glb");
            
            loader.load('/dummy3.glb', function (gltf) {
                    console.log("¡Modelo cargado exitosamente!");
                    const model = gltf.scene;
                    
                    // Centrar y escalar el modelo
                    const box = new THREE.Box3().setFromObject(model);
                    const size = box.getSize(new THREE.Vector3());
                    const center = box.getCenter(new THREE.Vector3());
                    
                    console.log("Tamaño del modelo:", size);
                    console.log("Centro del modelo:", center);
                    
                    model.position.sub(center);
                    
                    // Auto-escalar
                    const maxDim = Math.max(size.x, size.y, size.z);
                    if (maxDim > 0) {
                        const scale = 3 / maxDim;
                        model.scale.set(scale, scale, scale);
                        console.log("Modelo escalado a:", scale);
                    }
                    
                    scene.add(model);
                    console.log("Modelo añadido a la escena");
                    
                    // Ajustar cámara
                    camera.position.set(0, maxDim, maxDim * 2);
                    camera.lookAt(0, 0, 0);
                    controls.update();
                },
                function (xhr) {
                    const percent = (xhr.loaded / xhr.total * 100);
                    console.log(Math.round(percent) + '% cargado');
                },
                function (error) {
                    console.error("Error al cargar el modelo:", error);
                    console.error("Detalles:", error.message);
                }
            );

            // Animación
            function animate() {
                requestAnimationFrame(animate);
                controls.update();
                renderer.render(scene, camera);
            }
            
            // Iniciar animación
            animate();
            console.log("Animación iniciada");

            // Ajuste de ventana
            window.addEventListener('resize', () => {
                const width = container.clientWidth;
                const height = container.clientHeight;
                
                renderer.setSize(width, height);
                camera.aspect = width / height;
                camera.updateProjectionMatrix();
                
                console.log("Ventana redimensionada:", width, "x", height);
            });
        });
    </script>
}
